import React from "react";

const Error = () => {
  return (
    <section>
      <p>Errors in here. Only known case now:</p>
      <p>...would be There are no players that meet the requirements.</p>
    </section>
  );
};

export default Error;
